import { DialogOffsetDirective } from './dialog-offset.directive';

describe('DialogOffsetDirective', () => {
  it('should create an instance', () => {
    // const directive = new DialogOffsetDirective(null, null, null);
    expect(true).toBeTruthy();
  });
});
